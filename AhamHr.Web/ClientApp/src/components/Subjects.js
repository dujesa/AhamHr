import axios from "axios";
import { useEffect } from "react";

const initialState = {
  subjects: [],
  loading: false,
};

const Subjects = () => {
  /*const {
    state: { ...initialState },
  };

  useEffect(() => {
    axios.get();
  },[]);*/
};

export default Subjects;
